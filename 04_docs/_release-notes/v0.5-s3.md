# Agenda hebdo + revues + SSE live + last-sync — sprint S3 livré

**Version** : `v0.5-s3` · **Date** : 25 avr. 2026
**GitHub** : milestone=`v0.5-s3` · branch=`docs/sprint-s3-kickoff` · PR=#112 · issues=#113, #114, #115, #116, #117, #118, #119, #120, #121, #122, #123

Sprint S3 du plan v0.5 livré en ~3 h chrono dogfood par binôme CEO + Claude (vélocité bien supérieure aux 11,1 j-dev planifiés). 11/11 issues closes (S3.00 → S3.10), 5 commits de `ce22641` à `990be37`, **75/75 tests verts**. 4 piliers : agenda + revues + SSE câblé front + Outlook autosync côté serveur. Smoke live validé sur poste CEO Windows.

## Nouveautés

- **agenda.html migré API** : grille hebdo lun-dim, `GET /api/events/week?week=YYYY-Www&with_tasks=true`, drag-drop natif HTML5 (pattern S2.03) → `PATCH /api/tasks/:id { due_at }` + refetch optimiste 200 ms, A11y clavier complète (aria-grabbed, aria-dropeffect, flèches + Espace), drawer detail event
- **revues/index.html migré API** : Big Rocks éditables CRUD inline avec contrainte applicative **max 3/sem (HTTP 400)**, bouton « Demander brouillon Claude » → `POST /api/weekly-reviews/:week/draft`, archives 12 dernières revues, deep link `?week=YYYY-Www`
- **SSE câblé front** : `EventSource('/api/cockpit/stream')` sur cockpit + tâches + agenda avec reconnexion exponentielle 1 s → 30 s plafond. Bus émet sur `task.created/.updated/.deleted` depuis routes mutatrices
- **Outlook autosync (côté serveur)** : `GET /api/system/last-sync` (mtime emails-summary.json, level ok/warn>4h/critical>24h) + injection automatique alerte cockpit `outlook_stale`
- **Auto-draft Claude** : `src/llm-draft.js` agrège tâches done + sessions arbitrage/soir + big rocks → prompt système rubric 6 critères (focus / ton / 200-400 mots / 3+ sources / top 3 demain / écarts) → markdown structuré 4 sections. Fallback offline si pas de clé API
- **POC service Windows S3.10** : `scripts/service-windows/install-service.js` (node-windows wrapper install/start/stop) + README + ADR template — test install à faire en dogfood admin CEO

## Améliorations

- **Doc API** `docs/API.md` +182 lignes (6 sections S3 : events/week, big-rocks, weekly-reviews, auto-draft, system, SSE) avec exemples curl complets
- ADR S3.00 (méthode + zéro localStorage rappelé) + ADR S3 livré dans `00_BOUSSOLE/DECISIONS.md`
- Restauration de 4 ADRs perdues par truncature historique (Timing & budget v0.5, Livrables externes : cadrage, 2 templates)

## Performance

- **75/75 tests verts** : 62 hérités + 11 nouveaux (3 weekly-reviews, 3 big-rocks, 1 events with_tasks, 2 weekly-reviews-draft, 3 system, 2 realtime-emit, 6 e2e-hebdo). Durée 28 s, isolation `AICEO_DB_OVERRIDE` systématique

## Breaking changes

- Schedule variance vs BASELINE : **-49 j** (livré 25/04 vs planifié 13/06). Sprint S4 peut démarrer dès que le CEO décide
