# Cockpit unifié + rituels matin/soir — sprint S2 livré

**Version** : `v0.5-s2` · **Date** : 25 avr. 2026
**GitHub** : milestone=`v0.5-s2` · branch=`release/v0.5-s2` · tag=`v0.5-s2` · PR=#111 · issues=#101, #102, #103, #104, #105, #106, #107, #108, #109, #110

Sprint S2 du plan v0.5 livré en avance par binôme CEO + Claude (gain 1,5 j du time-box spike SSE réinvesti en e2e). 10/10 issues fermées, PR #111 mergée, 11 commits de `accea60` à `6f4e6e8`, **55/55 tests verts**. Cockpit live + rituels matin/soir + spike SSE.

## Nouveautés

- **4 pages migrées** sur l'API : `index.html` · `arbitrage.html` · `evening.html` · `taches.html` — **zéro localStorage applicatif**
- **Cockpit live** : `GET /api/cockpit/today` (counters SQL + alertes overdue / stale / big rocks manquants)
- **Rituels matin/soir** : arbitrage top 3 / délégation / report + bilan humeur·énergie + **streak persistant**
- **Vue Eisenhower 2×2** dans `taches.html` + drag & drop natif sur `arbitrage.html` (3 colonnes faire / déléguer / différer)
- **IA décisions** : `POST /api/decisions/:id/recommend` (Anthropic + fallback offline)
- **Spike SSE livré** : bus EventEmitter + `GET /api/cockpit/stream` + heartbeat 25 s — non câblé front (différé S3.05)

## Améliorations

- **Doc API complète** `docs/API.md` · **38 exemples curl** + smoke-test 1 commande
- Migration `data/appweb.json` → SQL via `scripts/migrate-from-appweb.js` + `check-migration`
- ADR SSE retenu vs WebSocket : `docs/SPIKE-WEBSOCKET.md` (latence inférieure, simpler stack, pas de proxy reverse à configurer)

## Performance

- **55/55 tests verts** : 49 unitaires + 6 intégration HTTP-boundary (Playwright sandboxé reporté S3+)
- Time-box spike SSE respecté : 1,5 j sur 3 j alloués · gain réinvesti en doc API + e2e
